# Copia a .env solo para emulador local. En producción usa Firebase Console → Functions → Configuración → Variables de entorno.

# SMTP (Gmail: contraseña de aplicación)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-correo@gmail.com
SMTP_PASS=tu-contraseña-de-aplicacion
SMTP_FROM="Dentisur Web" <tu-correo@gmail.com>

# Opcional: no enviar correo real (solo log)
# SKIP_EMAIL=true
