# Variables para la API de agendar cita (ruta /api/agendar-cita en Next.js).
# En Vercel: Settings → Environment Variables. Añade estas y redeploy.

# SMTP (Gmail: contraseña de aplicación)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-correo@gmail.com
SMTP_PASS=tu-contraseña-de-aplicacion
SMTP_FROM="Dentisur Web" <tu-correo@gmail.com>

# Opcional: no enviar correo real (solo log)
# SKIP_EMAIL=true
